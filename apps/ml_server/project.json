{
  "name": "ml_server",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": ". .venv/bin/activate && uvicorn main:app --reload --host=0.0.0.0 --port=8000",
        "cwd": "apps/ml_server"
      }
    }
  }
}
